require_relative "game"

describe Hangman do
  let(:game) { Hangman.new}

    it "is writeable word and letters" do
      game

    end

    #it "returns correct length of dashes" do
    #  expect(game.return_length(array_word)).to match_array("-------")
    #end

    #it "gives them the correct number of guesses" do
    #  expect(game.guess_count(array_word)).to eq array_word.length
    #end

    #it "lets user know if they guess a correct letter" do
    #  expect(game.guess_word).
    #end
  end
